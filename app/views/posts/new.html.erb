<h1>New post</h1>

<%= form_for @post do |f| %>
	<p>
		<%= f.label :title %><br>
		<%= f.text_field :title, class: 'form-control' %>
	</p>

	<p>
	<%= f.label :body %><br>
	<%= f.text_area :body %>
	</p>

	<%= f.submit class: "btn btn-primary" %>

<% end %>